<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>GitQuest</title>
    <link rel="stylesheet" href="/public/tailwind.css">
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
    <style>
        body {
            background-color: #f6f8ff;
            font-family: "Space Mono", monospace;
        }

        .card-container {
            display: flex;
            flex-wrap: wrap;
            margin-top: 10px;
            margin-bottom: 30px;
        }

        .profile-image {
            align-self: center;
            margin-right: 20px;
            width: 120px;
            height: 120px;
            border-radius: 50%;
            object-fit: cover;
        }

        .title-container {
            display: flex;
            justify-content: center;
            align-items: center;
            height: 80px;
        }

        .title-link {
            font-size: 3rem;
            color: #4b6a9b;
            font-weight: bold;
            text-decoration: none;
            margin-bottom: 0;
        }

        .joined-date {
            font-size: 1rem;
            color: #4b6a9b;
            font-weight: bold;
        }

        @media (max-width: 640px) {

            body {
                background-color: #f6f8ff;
                font-family: "Space Mono", monospace;
            }

            .card-container {
                flex-direction: column;
                align-items: center;
            }

            .profile-image {
                margin-right: 0;
                margin-bottom: 20px;
            }

            .flex-col {
                width: 100%;
            }

            .grid {
                grid-template-columns: repeat(auto-fit, minmax(150px, 1fr));
            }

            .title-container {
                flex-direction: column;
                justify-content: center;
            }
        }

        @media (max-width: 640px) {

            body {
                background-color: #f6f8ff;
                font-family: "Space Mono", monospace;
            }

            .card-container {
                flex-direction: column;
                align-items: center;
            }

            .profile-image {
                margin-right: 0;
                margin-bottom: 20px;
            }

            .flex-col {
                width: 100%;
            }

            .grid {
                grid-template-columns: repeat(auto-fit, minmax(150px, 1fr));
            }

            .title-container {
                flex-direction: column;
                align-items: center;
            }
        }
    </style>
</head>

<body>
    <div class="title-container mt-8">
        <div>
            <a href="/" class="title-link">
                GitQuest
            </a>
        </div>
    </div>
    <div class="container mx-auto max-w-6xl flex justify-center items-center">
        <div class="max-w-4xl mt-8 bg-white rounded-lg shadow-md overflow-hidden p-8">
            <div class="card-container">
                <img src="<%= user.avatar_url %>" alt="<%= user.name %>" class="profile-image">
                <div class="flex flex-col flex-grow">
                    <h1 class="text-xl font-bold mb-2">
                        <p class="mb-2">
                            <a href="https://github.com/<%= user.login %>" target="_blank"
                                class="text-blue-500 hover:underline text-3xl mb-2">
                                <%= user.login %>
                            </a>
                        <p class="mb-2">
                            <%= user.name %>
                        </p>
                        <p class="joined-date">
                            Joined: <%= user.created_at.slice(0, 10) %>
                        </p>
                        </p>
                    </h1>
                    <p class="text-gray-700 mb-2">
                        <%= user.bio %>
                    </p>
                    <div class="grid grid-cols-3 gap-4">
                        <div class="rounded-lg shadow-md px-4 py-6" style="background-color: #f6f8ff;">
                            <h2 class="text-lg font-bold mb-2">Followers</h2>
                            <p class="text-gray-700">
                                <%= user.followers %>
                            </p>
                        </div>
                        <div class="rounded-lg shadow-md px-4 py-6" style="background-color: #f6f8ff;">
                            <h2 class="text-lg font-bold mb-2">Following</h2>
                            <p class="text-gray-700">
                                <%= user.following %>
                            </p>
                        </div>
                        <div class="rounded-lg shadow-md px-4 py-6" style="background-color: #f6f8ff;">
                            <h2 class="text-lg font-bold mb-2">Repositories</h2>
                            <p class="text-gray-700">
                                <%= user.public_repos %>
                            </p>
                        </div>
                    </div>
                    <% if (user.blog) { %>
                        <p class="text-gray-700 mt-4">
                        <h2 class="text-lg font-bold mb-2">Blog:</h2>
                        <a href="<%= user.blog %>" class="text-blue-500 hover:underline">
                            <%= user.blog %>
                        </a>
                        </p>
                        <% } %>
                </div>
            </div>
        </div>
    </div>
</body>

</html>